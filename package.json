{
  "name": "threads-outreach-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production Threads outreach and lead-generation agent for AI agents, chatbots and business automation",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "test": "npm run build && node --test dist/src/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {"node": ">=22"},
  "dependencies": {"pg": "8.23.0"},
  "devDependencies": {"@types/node": "22.20.1", "@types/pg": "8.23.1", "typescript": "5.9.3"}
}
